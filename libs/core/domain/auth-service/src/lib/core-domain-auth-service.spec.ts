import { coreDomainAuthService } from './core-domain-auth-service';

describe('coreDomainAuthService', () => {
  it('should work', () => {
    expect(coreDomainAuthService()).toEqual('core-domain-auth-service');
  });
});
