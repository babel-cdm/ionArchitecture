import { coreDataPluginNativeHttp } from './core-data-plugin-native-http';

describe('coreDataPluginNativeHttp', () => {
  it('should work', () => {
    expect(coreDataPluginNativeHttp()).toEqual('core-data-plugin-native-http');
  });
});
