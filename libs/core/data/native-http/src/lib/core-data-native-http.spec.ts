import { coreDataNativeHttp } from './core-data-native-http';

describe('coreDataNativeHttp', () => {
  it('should work', () => {
    expect(coreDataNativeHttp()).toEqual('core-data-native-http');
  });
});
