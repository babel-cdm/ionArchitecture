import { coreDataAuthNativeHttp } from './core-data-auth-native-http';

describe('coreDataAuthNativeHttp', () => {
  it('should work', () => {
    expect(coreDataAuthNativeHttp()).toEqual('core-data-auth-native-http');
  });
});
