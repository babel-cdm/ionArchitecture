export const LOGIN_ROUTES = {
  LOGIN: '',
};

import { APP_ROUTES_GLOBAL } from "@app/presentation/app-routes";

const BASE = APP_ROUTES_GLOBAL.LOGIN;
export const LOGIN_ROUTES_GLOBAL = {
  HOME: `${BASE}`,
};
